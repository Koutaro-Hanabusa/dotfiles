# dotfiles 作業指示

このリポジトリは、Home Manager で個人の開発環境を管理する dotfiles である。

## 作業範囲

- `home-manager/` 配下を設定の正本として扱う。編集前に既存の設定と参照先を確認する。
- `home-manager/agents/` は共有 Agent Skills の置き場であり、個人の常時指示は置かない。
- ランタイム状態、キャッシュ、認証情報、未依頼の既存変更をコミットに含めない。

## Home Manager

- `.nix` ファイルを変更したら、コミット前に `home-manager switch --flake ~/dotfiles` を実行する。
- `mkOutOfStoreSymlink` で管理している設定ファイルは、通常は編集内容が即時反映される。実際のリンク先を確認してから完了とする。
- 設定の変更後は、必要な適用コマンドまたは対象ファイルの実体確認を行う。

## Git

- 変更後は `git diff --check` と `git status` で確認し、依頼されたファイルだけをステージする。
- このリポジトリの変更は検証後にコミットして GitHub へプッシュする。
- 既存の未関連変更がある場合は、それを巻き戻したり、依頼した変更と一緒にコミットしたりしない。

共通の応答ルールは各クライアントの指示ファイル、開発原則は対応する Skill を正本とする。
