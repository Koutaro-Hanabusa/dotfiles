#!/usr/bin/env bash
# Stop hook placeholder.
#
# The previous blocking reminder polluted later command text in Claude sessions.
# Codex now receives the nb-knowledge reminder through SessionStart context.

exit 0
